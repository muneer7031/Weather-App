{{!-- <!-- viewdata.hbs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Data</title>
</head>
<body>
    <h1>Country Data</h1>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Capital</th>
                <th>Memberships</th>
                <th>Total GDP</th>
                <th>Inflation</th>
                <th>Unemployment</th>
                <th>Government</th>
            </tr>
        </thead>
        <tbody>
            {{#each data}}
            <tr>
                <td>{{this.name}}</td>
                <td>{{this.capital}}</td>
                <td>{{this.memberships}}</td>
                <td>{{this.gdp_total}}</td>
                <td>{{this.inflation}}</td>
                <td>{{this.unemployment}}</td>
                <td>{{this.government}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html> --}}


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Data</title>
    <style>
        /* Body styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #006D77; /* Set background color to dark theme */
            color: #EDF6F9; /* Set text color to light theme */
            margin: 0;
            padding: 0;
        }

        /* Header styles */
        h1 {
            text-align: center;
            color: #EDF6F9; /* Set text color of the heading to light theme */
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2);
            padding: 20px 0;
        }

        /* Table styles */
        table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            background-color: #83C5BE; /* Set table background color */
        }

        table th,
        table td {
            padding: 10px;
            text-align: center;
            color: #EDF6F9; /* Set table data color */
        }

        table th {
            background-color: #4CAF50;
            color: white;
        }

        table tr:nth-child(even) {
            background-color: #FFDDD2; /* Set background color of even rows */
        }

        table tr:nth-child(odd) {
            background-color: #E29578; /* Set background color of odd rows */
        }

        table tr:nth-child(even):hover {
            background-color: #EDF6F9; /* Change hover color for even tr elements */
        }

        table tr:hover {
            background-color: black; /* Change background color to black on hover */
        }

        /* Set color of even td elements */
        table tr:nth-child(even) td {
            color: #280003; /* Set color of even td elements */
        }

        /* Footer styles */
        footer {
            text-align: center;
            padding: 20px 0;
            background-color: #333;
            color: #EDF6F9; /* Text color */
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2);
            position: fixed;
            bottom: 0;
            width: 100%;
        }

        /* Define the blink animation */
        @keyframes blink {
            0%, 100% {
                visibility: hidden;
            }
            50% {
                visibility: visible;
            }
        }

        /* Apply the blink animation every 3 seconds */
        footer .blink {
            animation: blink 3s infinite;
        }

        /* Define animation keyframes for fade-in effect */
        @keyframes fadeIn {
            0% {
                opacity: 0;
            }
            100% {
                opacity: 1;
            }
        }

        /* Apply animation to highlighted text */
        footer .highlight {
            animation: fadeIn 2s ease-in-out infinite; /* Make animation continuous */
            font-size: inherit; /* Keep font size the same */
            color: #ffcc00; /* Set text color to yellow */
        }
    </style>
</head>

<body>
    <h1>Country Data</h1>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Capital</th>
                <th>Memberships</th>
                <th>Total GDP</th>
                <th>Inflation</th>
                <th>Unemployment</th>
                <th>Government</th>
            </tr>
        </thead>
        <tbody>
            {{#each data}}
            <tr>
                <td>{{this.name}}</td>
                <td>{{this.capital}}</td>
                <td>{{this.memberships}}</td>
                <td>{{this.gdp_total}}</td>
                <td>{{this.inflation}}</td>
                <td>{{this.unemployment}}</td>
                <td>{{this.government}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

    <footer>
        <p>© Developed with ❤️ by <span class="blink highlight">MOHAMMED MUNEER</span>. All rights reserved.</p>
    </footer>
</body>

</html>
